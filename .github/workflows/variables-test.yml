on:
  workflow_dispatch:
    inputs:
      SKIP:
        description: "Optional value for overriding"
        required: false
        type: boolean

jobs:
  test:
    uses: ./.github/workflows/reusable-wf.yml
    with:
      SKIP: ${{ inputs.SKIP }}