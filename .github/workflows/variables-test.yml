on:
  workflow_dispatch:
    inputs:
      SKIP:
        description: "Optional value for overriding"
        required: false
        type: boolean

jobs:
  test:
    uses: ./.github/workflows/reusable-wf.yml
    with:
      SKIP: ${{ inputs.SKIP }}
  skipedibap:
    name: skipedibap
    needs: test
    runs-on: ubuntu-latest
    if: ${{ !cancelled() }}
    steps:
      - name: "x"
        run: echo "Skipped."
