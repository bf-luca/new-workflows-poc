on:
  workflow_call:
    inputs:
      SKIP:
        required: true
        type: boolean

jobs:
  deploy-docu-prod:
    name: prod
    runs-on: ubuntu-latest
    if: ${{ inputs.SKIP == true }}
    steps:
      - name: test
        run: echo "Hi"